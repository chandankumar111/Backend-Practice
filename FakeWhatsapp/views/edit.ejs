<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Messages</title>
</head>
<body>
    <h3>Edit Your Messages : <%= chat._id %></h3>
    <p>This chat is sent from <b><%= chat.from %></b> to <b><%= chat.to %></b></p>
    <form action="/chats/<%= chat._id %>?_method=PUT" method="post">
        <textarea name="msg" id="message" rows="6" cols="40"><%= chat.msg %></textarea><br>
        <button>Edit</button>
    </form>

</body>
</html>